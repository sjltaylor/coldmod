syntax = "proto3";

import "google/protobuf/Empty.proto";

package proto;

message Trace {
  string path = 1;
  uint32 line = 2;
}

service TracingCollector {
  rpc trace(stream Trace) returns (google.protobuf.Empty);
}
