syntax = "proto3";

import "google/protobuf/empty.proto";

package coldmod_msg.proto.trace;

message Trace {
  string digest = 1;
  uint64 thread_id = 2;
  uint64 process_id = 3;
}

service Traces {
  rpc collect(stream Trace) returns (google.protobuf.Empty);
}
