syntax = "proto3";

import "google/protobuf/empty.proto";

package coldmod_msg.proto.source;

message Source {
  string path = 1;
  uint32 line = 2;
  string name = 3;
  optional string class_name = 4;
}

service SourceDaemon {
  rpc collect(stream Source) returns (google.protobuf.Empty);
}
